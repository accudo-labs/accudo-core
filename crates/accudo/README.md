# Accudo Command Line Interface (CLI) Tool

The `accudo` tool is a command line interface (CLI) for debugging, development, and node operation.

See [Accudo CLI Documentation](https://accudo.dev/tools/accudo-cli/) for how to install the `accudo` CLI tool and how to use it.
