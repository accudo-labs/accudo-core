apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "accudo-validator.serviceAccountName" . }}
  labels:
{{ include "accudo-validator.labels" . | nindent 4 }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "accudo-validator.fullname" . }}-validator
  labels:
    {{- include "accudo-validator.labels" . | nindent 4 }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "accudo-validator.fullname" . }}-fullnode
  labels:
    {{- include "accudo-validator.labels" . | nindent 4 }}

--- 

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "accudo-validator.fullname" . }}-haproxy
  labels:
    {{- include "accudo-validator.labels" . | nindent 4 }}
