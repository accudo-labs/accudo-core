{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "accudo-monitoring.serviceAccountName" . }}
  labels:
    {{ include "accudo-monitoring.labels" . | nindent 4 }}
{{- end -}}
